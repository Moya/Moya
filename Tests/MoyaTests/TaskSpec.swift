import Quick
import Moya
import Nimble
import Foundation

final class TaskSpec: QuickSpec {

    override func spec() {

        let bodyEncodable: [String: String] = ["Encodable": "Body"]
        let queryEncodable: [String: String] = ["Encodable": "Query"]

        
    }
}
